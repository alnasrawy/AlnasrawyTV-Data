# secrets
.env

# python
__pycache__/
*.py[cod]
.venv/
venv/

# sqlite state (step 3) — state.db IS tracked so GitHub Actions can
# persist the "already sent" set between runs; only the aux files are ignored
*.db-wal
*.db-shm

# generated artifacts
out_imgs/
demo_out/
matches_*.json

# logo cache (downloaded once, re-fetchable)
renderer/assets/logos/

# os / ide
.DS_Store
Thumbs.db
.idea/